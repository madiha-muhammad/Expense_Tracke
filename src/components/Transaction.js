import React from 'react';

export const Transaction = ({ Transaction }) => {
    return (
        <li>
            <span>{item.desc}</span>
            <span>{item.amount}</span>
        </li>
    )
}